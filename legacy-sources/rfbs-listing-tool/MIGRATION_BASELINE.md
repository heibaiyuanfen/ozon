# RFBS 上品工具迁移基线

该目录是从原 RFBS 上品工具复制的只读源码快照，用于逐函数、逐字段和逐测试迁移到 `desktop-next`。

复制日期：2026-08-25。

仅包含 Python 源码、测试、示例配置、旧 README 和 PyInstaller spec。真实配置、API 导出、浏览器会话、运行状态、任务数据、SQLite/Excel 数据、缓存、依赖和输出均未复制。

迁移状态和每次更新记录统一维护在项目根目录的 `PROJECT_ARCHITECTURE_AND_MIGRATION.md`。
