# Ozon 广告分析软件：UI 与广告模块改造说明

## 本次交付范围

本次改造以用户提供的三张参考图为视觉和信息架构基准，完成了工作台风格统一、订单中心跨境开放、店铺币种切换以及广告分析页重构。所有改动均保留了现有的 SQLite 缓存、Seller / Performance 同步和多店铺隔离模型。

| 模块 | 已完成变更 | 数据与显示口径 |
| --- | --- | --- |
| 工作台 | 保留浅灰背景、白色卡片、紧凑侧栏、日期筛选与状态栏的现代化工作台结构；总览实际启动并完成截图核验。 | 本土店核心金额显示为 `₽`。 |
| 店铺币种 | 新增统一的 UI 金额换算与显示入口。 | 本土店保留 Seller 原始 RUB；跨境店将 Seller 原始 RUB 按当前店铺 `1 CNY = X RUB` 汇率转换为 `CNY` 后显示。 |
| 订单中心 | 订单页面更名为“订单中心”，不再因跨境店类型被禁用；订单金额、基础配送/件、基础配送合计列随店铺类型自动更新单位。 | 跨境店真实 Tk 冒烟测试已验证三列均显示 `CNY`。订单数据仍来自各店独立缓存的 `posting_routes`。 |
| 广告数据 | 页面改造为“指标卡片＋真实数据范围＋广告转化漏斗＋广告计划表现”结构。 | 全店广告汇总只计 `ad_daily.sku=''` 的活动总计，避免与 SKU 归因明细重复计入。 |
| 广告指标 | 新增平均 CTR、平均 CPC、付费 ROAS、加购、点击→下单转化率、每单广告费、ACOS。 | CPC = 消耗 ÷ 点击；点击→下单 = 广告订单 ÷ 点击；每单广告费 = 消耗 ÷ 广告订单；ROAS = 广告销售额 ÷ 消耗。 |

## 关键实现位置

| 文件 | 关键实现 |
| --- | --- |
| `ozon_app/ui.py` | 店铺币种显示、跨境订单中心访问、订单金额列标题、广告数据页面、广告漏斗和广告计划表渲染。 |
| `ozon_app/database.py` | `advertising_summary()` 汇总接口，以及 `top_campaigns()` 的加购、CPC、转化率、每单广告费字段。 |
| `tests/test_database.py` | 新增广告汇总与漏斗指标回归测试。 |
| `ozon_app/config.py`、`api_bundle.py`、`exports.py`、`scheduler.py` | 原压缩包中解压为 0 字节的四个模块已按现有调用接口与测试恢复，确保应用能够启动并保持配置迁移、报告导出、补货表及计划任务入口可用。 |

## 验证结果

完整单元测试套件已通过：**96 项通过，1 项因可选 PDF 读取依赖缺失而跳过**。应用已在虚拟桌面中实际启动并截图核验经营总览和广告数据页；跨境店订单中心的访问权限、CNY 列标题也已通过独立 Tk 冒烟测试。

## 后续迭代建议

后续如需继续扩展广告模块，可以优先增加按商品 / 活动类型 / 站点的筛选、趋势对比、预算与目标 ACOS 预警，以及从 Ozon API 获取更细粒度投放字段后的 CTR、CPC、转化率分层对比。由于当前广告漏斗仅依赖已有的曝光、点击、加购和订单日报字段，因此不会伪造未同步数据。
