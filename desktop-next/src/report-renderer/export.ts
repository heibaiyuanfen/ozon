export async function exportReportPng(element:HTMLElement,name:string){const {default:html2canvas}=await import("html2canvas"),canvas=await html2canvas(element,{scale:2,useCORS:true,backgroundColor:getComputedStyle(element).backgroundColor||"#fff"}),a=document.createElement("a");a.download=`${name}.png`;a.href=canvas.toDataURL("image/png");a.click();}
export async function exportReportPdf(element:HTMLElement,name:string,orientation:"portrait"|"landscape"){const [{default:html2canvas},{jsPDF}]=await Promise.all([import("html2canvas"),import("jspdf")]),canvas=await html2canvas(element,{scale:2,useCORS:true,backgroundColor:"#fff"}),pdf=new jsPDF({orientation,unit:"mm",format:"a4"}),pageWidth=pdf.internal.pageSize.getWidth(),pageHeight=pdf.internal.pageSize.getHeight(),height=canvas.height*pageWidth/canvas.width,img=canvas.toDataURL("image/jpeg",.96);let offset=0;while(offset<height){if(offset>0)pdf.addPage();pdf.addImage(img,"JPEG",0,-offset,pageWidth,height,undefined,"FAST");offset+=pageHeight;}pdf.save(`${name}.pdf`);}
